import Layout from "../components/layout/Layout"

interface Props {}

const aboutme = ({}: Props) => {
  return (
    <Layout>
      <div className="container"> </div>
    </Layout>
  )
}

export default aboutme